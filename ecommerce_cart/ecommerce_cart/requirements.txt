# ============================================================
#  Ecommerce Cart API — Python Dependencies
#  Install: pip install -r requirements.txt
# ============================================================

# ── Web framework ────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6          # ASGI server (websockets + httptools)

# ── Database ─────────────────────────────────────────────────
sqlalchemy==2.0.35                 # ORM
pymysql==1.1.1                     # Pure-Python MySQL driver
cryptography==43.0.1               # Required by pymysql auth plugin
alembic==1.13.3                    # DB schema migrations

# ── Validation & settings ────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2           # BaseSettings + .env support
python-dotenv==1.0.1               # .env file loader

# ── Dev / testing (uncomment to use) ─────────────────────────
# pytest==8.3.3
# httpx==0.27.2                    # Async test client for FastAPI
# pytest-asyncio==0.24.0